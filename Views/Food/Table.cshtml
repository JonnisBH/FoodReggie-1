@model FoodViewModel

<a href="/Food/Grid">Grid View</a>

<div>
    <h1>@ViewBag.CurrentViewName</h1>
    <table>
        <tr>
            <th>ItemId</th><th>Name</th><th>Nutrition values per 100 gram</th>
        </tr>
    @foreach (var food in Model.Foods){
        <tr>
            <td>@food.FoodId</td>
            <td>@food.Name</td>
            <td>
                @food.Calories.ToString("0 kcal")
                @food.Protein.ToString("0.0 g")
                @food.Carbohydrates.ToString("0.0 g")
                @food.Fats.ToString("0.0 g")    
            </td>
        </tr>
    }
    </table>
</div>

